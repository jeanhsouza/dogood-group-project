import PostsList from "./posts";
import { StyledProfile } from "./style";

const Profile = () => {
	return (
    <StyledProfile>
        <PostsList/>
    </StyledProfile>
    );
};

export default Profile;
